{
  "name": "@epn/core",
  "version": "0.1.0",
  "private": true,
  "description": "EPN 水平内核：查询编排器 / 混合检索 / 知识图谱 / 诊断引擎（企业无关）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "eval": "tsx src/evaluation/runner.ts"
  },
  "dependencies": {}
}
